CF deployment automation
========================

Basic example that installs CF CLI package and creates config file for it. You need to be sighed in via CF UAA SSO.
Repo shuld be configured to use stage2.
